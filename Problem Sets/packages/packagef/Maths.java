package packagef;

public class Maths
{
    public int max(int a,int b)
    {
        return (a >= b) ? a:b;
    }

    public double power(double base, double expo)
    {
        return Math.pow(base,expo);
    }
}
